{
  "name": "ng2-store",
  "version": "1.0.0",
  "description": "An easy to use Data Store for Angular 2",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib && mkdir lib && tsc",
    "watch": "rm -rf lib && mkdir lib && tsc -w"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^1.6.2"
  }
}
